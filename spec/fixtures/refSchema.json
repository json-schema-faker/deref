{
  "type": "object",
  "required": ["key"],
  "properties": {
    "key": {
      "$ref": "#/definitions/prop"
    }
  },
  "definitions": {
    "sub": {
      "allOf": [
        {
          "type": "string"
        },
        {
          "id": "a/b/c",
          "$ref": "../x#/y/z"
        }
      ]
    },
    "prop": {
      "id": "a/b",
      "$ref": "c.json"
    }
  }
}
