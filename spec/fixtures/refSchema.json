{
  "type": "object",
  "required": ["key"],
  "properties": {
    "key": {
      "$ref": "#/definitions/prop"
    }
  },
  "definitions": {
    "id": "a/b",
    "sub": {
      "id": "c/d",
      "allOf": [
        {
          "type": "string"
        },
        {
          "$ref": "../x#/y/z"
        }
      ]
    },
    "prop": {
      "$ref": "c.json"
    }
  }
}
